<Window x:Class="MultiLogViewer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MultiLogViewer"
        mc:Ignorable="d"
        Title="Multi Log Viewer" Height="450" Width="800">
    <DockPanel>
        <Menu DockPanel.Dock="Top">
            <MenuItem Header="ファイル(_F)">
                <MenuItem Header="開く(_O)" Command="{Binding OpenFileCommand}" />
                <Separator />
                <MenuItem Header="終了(_X)" Click="ExitMenuItem_Click" />
            </MenuItem>
            <MenuItem Header="設定(_S)">
                <MenuItem Header="設定画面を開く" Click="OpenSettingsMenuItem_Click"/>
            </MenuItem>
        </Menu>

        <ListView ItemsSource="{Binding LogEntries}">
            <ListView.View>
                <GridView>
                    <GridViewColumn Width="150">
                        <GridViewColumnHeader Click="TimestampHeader_Click">Timestamp</GridViewColumnHeader>
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Timestamp}" />
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    <GridViewColumn Header="Level" DisplayMemberBinding="{Binding Level}" Width="80"/>
                    <GridViewColumn Header="Message" DisplayMemberBinding="{Binding Message}" Width="500"/>
                </GridView>
            </ListView.View>
        </ListView>
    </DockPanel>
</Window>