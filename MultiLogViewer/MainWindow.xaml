<Window x:Class="MultiLogViewer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MultiLogViewer"
        mc:Ignorable="d"
        Title="Multi Log Viewer" Height="450" Width="800"
        Background="{StaticResource WindowBackgroundBrush}">
    <DockPanel>
        <Menu DockPanel.Dock="Top">
            <MenuItem Header="ファイル(_F)">
                <MenuItem Header="開く(_O)" Command="{Binding OpenFileCommand}" />
                <Separator />
                <MenuItem Header="終了(_X)" Click="ExitMenuItem_Click" />
            </MenuItem>
            <MenuItem Header="設定(_S)">
                <MenuItem Header="設定画面を開く" Click="OpenSettingsMenuItem_Click"/>
            </MenuItem>
        </Menu>

        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="5">
            <TextBlock Text="Filter:" VerticalAlignment="Center" Margin="0,0,5,0"/>
            <TextBox Text="{Binding FilterText, UpdateSourceTrigger=PropertyChanged}" Width="200" />
        </StackPanel>

                                <DataGrid ItemsSource="{Binding LogEntriesView}">

                                    <DataGrid.Columns>

                                        <DataGridTextColumn Header="Timestamp" Binding="{Binding Timestamp}" Width="150" SortMemberPath="Timestamp"/>

                                        <DataGridTextColumn Header="Level" Binding="{Binding Level}" Width="80" SortMemberPath="Level"/>

                                        <DataGridTextColumn Header="Message" Binding="{Binding Message}" Width="*" SortMemberPath="Message"/>

                                        <DataGridTextColumn Header="User" Binding="{Binding AdditionalData[user]}" Width="80" SortMemberPath="AdditionalData[user]"/>

                                        <DataGridTextColumn Header="Session" Binding="{Binding AdditionalData[session]}" Width="80" SortMemberPath="AdditionalData[session]"/>

                                    </DataGrid.Columns>

                                </DataGrid>
    </DockPanel>
</Window>